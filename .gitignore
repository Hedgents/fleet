# Rust build artifacts
/target/
**/*.rs.bk

# OS / editor junk
.DS_Store
.idea/
.vscode/
*.swp

# Secrets / runtime state
*.token
fleet.json
*-wallet.json
*-role.key
*.key
.env
.env.local
# Daemon stamp files (e.g. stablefloor.last)
*.last
# Daemon runtime state — sqlite journal (multiply), epoch logs (node-enterprise)
*-journal.sqlite
*-journal.sqlite-shm
*-journal.sqlite-wal
zerox1-epoch-*.cbor
# Daemon pnl telemetry log
*-pnl.jsonl
# Researcher signal telemetry log (M9)
*-signals.jsonl

# Dashboard server runtime state
dashboard.sqlite
dashboard.sqlite-shm
dashboard.sqlite-wal
*.sqlite
*.sqlite-shm
*.sqlite-wal

# Operator soak workspace (when run from inside the repo)
/01fi-soak/
/soak/
/secrets/
/logs/

# Local installs left from manual testing
/dist/

# ── Internal planning docs (kept locally, not published) ────────────────────
# Strategy, risk model, ADV pitch context, deployment plans — these stay on
# the maintainer's machine and are not part of the public repo.
/PLAN.md
/PORTFOLIO_STRATEGY.md
/PROTOCOLS.md
/RISK_MODEL.md
/FLEET_PAIRING_PERSONAL.md
/FLEET_CONFIG_ENTERPRISE.md
